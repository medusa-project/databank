= form_for @identity do |f|
  - if @identity.errors.any?
    #error_explanation
      %h2= "#{pluralize(@identity.errors.count, "error")} prohibited this identity from being saved:"
      %ul
        - @identity.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
