.modal.fade(id="loadingModal")
  .modal-dialog
    .modal-content
      .modal-header
        %button(type="button" class="close" data-dismiss="modal" aria-label="Close")
          &times;
        .text-center
          Processing
      .modal-body
        .loading_image.text-center
          %span(class="far fa-spinner fa-spin fa-4x" )
