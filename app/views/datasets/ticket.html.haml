%h4 Ticket
%a(href="https://help.uillinois.edu/TDWorkManagement" target="_blank") TeamDynamix tickets 
- if @dataset.ticket_id.present?
  %a(href="#{@dataset.ticket_url}" target="_blank")
    View in TeamDynamix
    = @dataset.ticket_id
- else
  %p No ticket available.

= @existing_ticket.to_yaml if @existing_ticket.present?