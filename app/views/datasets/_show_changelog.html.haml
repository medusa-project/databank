%table.table.table-striped

  -@dataset.changelog_array.each do |change|
    %tr.row
      %td
        ="#{change.auditable_type}"
      %td
        ="#{change.action}: #{change.audited_changes}"
      %td
        ="#{change.created_at.iso8601.to_s}"